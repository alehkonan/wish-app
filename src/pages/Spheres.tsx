import { Box } from '@mui/material';
import React, { FC } from 'react';
import { SpheresWidget } from '../widgets/Spheres';

export const SpheresPage: FC = () => {
  return (
    <Box>
      <SpheresWidget />
    </Box>
  );
};
